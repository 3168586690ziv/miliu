# SUBAGENT-RULES.md — 资源探测：子智能体委派规则（强制）

> 本文件属于**项目本身**，与根目录 `AGENTS.md` 同等效力，是强制规则而非建议。
> 任何 AI（无论何种模型、任务、上下文）在**派发子智能体之前**，必须先读完本文件并遵守。
> 本规则长期有效；修改本文件前必须获得用户明确同意。
> 入口：`AGENTS.md` 第 0 节指向本文件——**先读 AGENTS.md，再读本文件**。

## 0. 总则

默认**自己做**。只有同时满足「不触碰禁派红线」+「命中该派判据」+「通过成本闸门」三条，
才允许派子智能体。每次准备派发前，先用一行写下判断结果：

    【委派决策】派 / 不派 · 一句话理由

没有这一行，就说明你没有判断，而是凭感觉。
若本环境不支持子智能体：直接自己做，**不降低任何验收标准**，不要因此跳过验证，也不要假装派过。

## 1. 先过禁派红线：触碰任意一条，直接自己做

| 编号 | 情形 | 结论 |
| --- | --- | --- |
| E | 一次 grep / 一次读文件就能拿到（已知文件、符号或值） | 自己做 |
| F | 需要当前对话上下文才能说清 | 自己做 |
| G | 方向会边做边变、需要来回交互 | 自己做 |
| H | 多个子任务会写同一个文件，或共享可变状态（生成文件、缓存、端口、数据库） | 自己做，或改成串行 |
| I | 要的是判断（是否通过、能不能上线），不是事实 | 自己做 |
| J | 不可逆或对外（commit / push / 装应用 / 发消息 / 删数据 / 花钱） | 自己做，或先问用户 |

红线优先于判据：哪怕某件事看起来很适合并行，只要碰到 H，就不许拆给多个子智能体。

## 2. 再看该派判据：命中任意一条才考虑派

**A. 噪音隔离** —— 拿到答案要读/跑一大堆东西，但答案本身一两句话能说清。
　例：查「谁引用了 X」要翻十几个文件；跑一个吐几千行的测试，只要退出码和两行摘要。

**B. 扇出并行** —— 有两个及以上子任务，各自都要多步，且互相**真正独立**。
　「独立」的硬标准（三条全满足才算）：不写同一个文件、不共享可变状态、不依赖对方结果。
　只是「看起来不相关」不算独立。

**C. 长耗时后台** —— 单件事要跑很久且不需要我盯着 → 用后台方式派发，派完继续干别的。

**D. 无偏见复核** —— 需要不带我预设的检查。只用来**提出可疑点**，不用来**签字放行**。
　子智能体不知道我期待什么答案，所以它适合当第二双眼睛；但它说「没问题」不等于没问题。

## 3. 最后过成本闸门

委派的固定成本 ≈ 一个全新上下文（数万 token）+ 写自包含 prompt 的时间。判断式：

> 这件事我亲自做，需要读/产生的量，是否**明显大于**「一个有独立上下文的人从头做一遍」的开销？

是 → 派。否 → 自己做。

已知反例，别重犯：为了查 6 个能一条命令拿到的数字而派 6 个子智能体——成本远高于自己做，纯亏。

## 4. 派发纪律

- prompt 必须**自包含**：绝对路径、背景、期望产物、输出格式、边界（不许做什么）。
  子智能体空白启动，看不到我们的对话，也不记得之前说过什么。
- prompt 里必须写明一句：**「本项目受根目录 `AGENTS.md` 与 `SUBAGENT-RULES.md` 约束，你必须遵守」**。
  子智能体不一定继承工作区规则，不能默认它会自己读到；要点名要求，并把它**不许做**的事写进 prompt。
- 明确要求它交回：**命令原文 + 退出码 + 关键输出摘录**。
  明确声明不接受「已完成」「通过」「应该没问题」这类无证据结论。
- 派完**不要自己重复做同一件事**，等它的结果回来。
- 只读的调研/搜索可以放心派；写操作自己来。

## 5. 收结果纪律

- 子智能体的输出一律**先当线索，不当结论**。
- 它报的每个关键数字、每条断言，凡是我自己能核的，**必须自己核一遍**再采信。
- 它说「通过」不算通过；我看到退出码 0 和原始输出才算通过。
- 它交回的结果与我的核对不符时，以我的核对为准，并在汇报里写明分歧。

## 6. 本项目的具体红线（子智能体一律不得触碰）

这些是本项目历史上真实踩过的坑，比通用规则更硬：

1. **不得跑 `--release-fix`**，不得调用 `scripts/build.sh --release-fix` /
   `scripts/build-release.sh --release-fix`。
2. **不得改动 `PROJECT_VERSION.json`**，尤其不得改 `fixRound`。修复轮数是项目历史事实，
   见 `AGENTS.md` 第 2 节；任何 AI 都无权递增。
3. **不得改 `AGENTS.md` 与 `SUBAGENT-RULES.md`**（需用户明确同意）。
4. **不得动 `resources/Resources/AppIcon.icns`**（用户的 Z 字图标，有 SHA-256 基线）。
5. **不得安装或改动 `/Applications/资源探测.app`**，不得强制终止用户正在运行的实例，
   不得删除下载记录、不得修改用户图标。
6. **不得执行 git 写操作**：`commit` / `push` / `checkout` / `branch` / `reset` / `tag`
   一律禁止；工作树保持未提交状态，由用户处理版本管理。
7. **不得为了「让测试通过」而删改、放宽测试断言**。宁可新增断言，不可删断言；
   断言若因结构调整必须改语义，必须单独说明「原断言 → 新断言 → 为什么不等于放水」。
8. **不得把一次普通编译说成一次新的修复轮**；普通构建、重复构建一律不递增轮数。
9. **不得用「应该没问题」替代实测**；做不到的事如实说做不到。
10. **不得改写 `build/generated/RDGeneratedVersion.h`** 的内容或绕过
    `scripts/build.sh` 手工拼装 App。

## 7. 本项目特有的并行陷阱（重点）

**`build/generated/RDGeneratedVersion.h` 是共享可变状态。**
`tests/Tests/repair.sh`、`tests/Tests/ui-experience.sh`、`tests/Tests/version.sh`、
`build/settings-probe/probe.sh` 等都会先跑 `scripts/generate-version.sh` 重算并覆写同一个
生成头；`version.sh` 还会额外跑一次完整 `build.sh`。

**因此把它们并行派给多个子智能体会互相踩。** 看起来它们是「独立的三条命令」，
但共享同一个生成产物 → 按第 1 节红线 **H**，必须串行执行。

同理：多个子智能体绝不可并行编辑 `src/App/ResourceDetectorApp.m` 等任何**同一个源文件**。
一个连贯的源码改动由主智能体**串行**完成——并行写同一文件必然互相覆盖，
最后编译不过还查不出是谁改坏的。

## 8. 汇报要求

向用户汇报时，凡引用了子智能体的结果，必须带上**命令原文 + 退出码 + 关键输出摘录**；
不能只写「子智能体确认通过」。区分【实测】（有命令、有输出）与【推论】，
未做到的项如实列出，不得用「应该没问题」糊弄。
